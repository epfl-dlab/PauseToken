# @package _global_

defaults:
  - train/online_star_exp/pause
  - override /rl_algorithm/policy/model/peft_config: null #peft is already there


run_name: "star_pause_tinyllama"

rl_algorithm:
  n_steps: 9

  policy:
    model:
      language_model:
        pretrained_model_name_or_path: /dlabscratch1/baldwin/pause2/PauseToken/logs/sft/runs/2024-12-04_14-50-01/final


trainer:
  disable_peft_first_inference: true #there is no peft here so I'll disable this