# @package _global_
defaults:
  - train/sft/default
  - override /rl_algorithm/policy/model/peft_config: null


run_name: "Llama1B_sft"

rl_algorithm:
  data_collator:
    add_context_to_response: true

  policy:
    model:
      language_model:
        pretrained_model_name_or_path: /dlabscratch1/public/llm_weights/meta-llama_Llama-3.2-1B

    generation:
      train:
        generation_config:
          pad_token_id: 128002

      test:
        generation_config:
          pad_token_id: 128002